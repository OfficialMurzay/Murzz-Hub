-- Load Rayfield
local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()

-- Create the Window
local Window = Rayfield:CreateWindow({
    Name = "Murzz Hub | AFSE",
    LoadingTitle = "Murzz Hub | AFSE",
    LoadingSubtitle = "by You",
    ConfigurationSaving = {
       Enabled = true,
       FolderName = nil, -- Create a custom folder for your hub settings
       FileName = "MurzzHubSettings"
    },
    Discord = {
       Enabled = false,
       Invite = "", 
       RememberJoins = true 
    },
    KeySystem = false -- Set to true if you want a key system
})

-- Home Tab
local HomeTab = Window:CreateTab("Home", 4483362458) -- The second argument is an image ID, optional

-- Live Stats Section
local LiveStatsSection = HomeTab:CreateSection("Live Stats")

-- Individual Stats Labels
local stats = {"Durability", "Agility", "Speed", "Sword", "Strength", "Chakra"}
local statsValues = {}

for _, stat in ipairs(stats) do
    -- Create label for the stat
    HomeTab:CreateLabel(stat)
    -- Create label for the value (starting at 0)
    local valueLabel = HomeTab:CreateLabel("0")
    statsValues[stat] = valueLabel
    -- Optional: separator
    HomeTab:CreateLabel("-----")
end

-- Auto Train Tab
local AutoTrainTab = Window:CreateTab("Auto Train", 4483362458)

-- Teleports Tab
local TeleportsTab = Window:CreateTab("Teleports", 4483362458)

-- Utilities Tab
local UtilitiesTab = Window:CreateTab("Utilities", 4483362458)

-- Example: Function to update a stat dynamically
local function UpdateStat(statName, newValue)
    if statsValues[statName] then
        statsValues[statName].Label.Text = tostring(newValue)
    end
end

-- Example usage
-- UpdateStat("Durability", 10)
-- UpdateStat("Agility", 5)
